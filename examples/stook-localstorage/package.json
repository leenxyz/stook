{
  "name": "example-stook-localstorage",
  "private": true,
  "scripts": {
    "start": "umi dev",
    "build": "echo",
    "prettier": "prettier --write '**/*.{js,jsx,tsx,ts,less,md,json}'",
    "test": "umi-test",
    "test:coverage": "umi-test --coverage"
  },
  "gitHooks": {
    "pre-commit": "lint-staged"
  },
  "lint-staged": {
    "*.{js,jsx,less,md,json}": [
      "prettier --write"
    ],
    "*.ts?(x)": [
      "prettier --parser=typescript --write"
    ]
  },
  "dependencies": {
    "@ant-design/pro-layout": "^6.5.0",
    "@umijs/preset-react": "1.x",
    "umi": "^3.4.22"
  },
  "devDependencies": {
    "@umijs/test": "^3.4.22",
    "lint-staged": "^10.0.7",
    "stook-localstorage": "^1.16.0",
    "yorkie": "^2.0.0"
  },
  "version": "1.15.0"
}
